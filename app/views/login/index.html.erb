<%= form_for :user, url: login_path do |f| %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.submit '登录' %>
    </p>
<% end %>
<%= link_to '注册', new_user_path, method: :get %>
